glm::mat4 MVP = ...;
float scale = ...;

gl::program prog;
prog << "simple.vs" << "simple.fs";
prog.link();

prog << uniform("MVP", MVP) << uniform("scale", scale);

alebo aj

prog << uniform(MVP_loc, MVP) << uniform(scale_loc, scale);

alebo

prog.uniform("MVP", MVP);
prog.uniform("scale", scale);


(?) aký prínos by mala implementácia op<<() pre uniformny ?

použitie členskej funkcie sa zdá byt prehladnejšie.


prog.attribute_pointer("position", ...)
prog.attribute_pointer(position_loc, ...)


